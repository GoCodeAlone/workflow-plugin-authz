# Minimal config exercising workflow-plugin-authz.
# Schema-validate with: wfctl validate --skip-unknown-types examples/minimal/config.yaml

version: 1

modules:
  - name: authz-provider
    type: authz.casbin
    config: {}

workflows:
  pipeline:
    steps:
      - name: example
        type: step.authz_check
        config: {}
